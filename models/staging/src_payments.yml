version: 2

sources:
  - name: raw
    description: Raw payment transaction dataset
    tables:
      - name: raw_payment_transactions
        description: Transaction-level payment records containing interchange and authorization attributes
